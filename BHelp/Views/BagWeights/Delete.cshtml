@model BHelp.Models.BagWeight
@{ Layout = null; }
<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>Delete</title>
    <style>
        #DeleteButton {
        background-color: red; color: white}
    </style>
</head>
<body>
    <h3>Are you sure you want to delete this?</h3>
    <div>
        <h4>Bag Weights</h4>
        <hr />
        <dl class="dl-horizontal">
            <dt>
                @Html.DisplayNameFor(model => model.APounds)
            </dt>

            <dd>
                @Html.DisplayFor(model => model.APounds)
            </dd>

            <dt>
                @Html.DisplayNameFor(model => model.BPounds)
            </dt>

            <dd>
                @Html.DisplayFor(model => model.BPounds)
            </dd>

            <dt>
                @Html.DisplayNameFor(model => model.EffectiveDate)
            </dt>

            <dd>
                @Html.DisplayTextFor(model => model.EffectiveDateString)
            </dd>

        </dl>

        @using (Html.BeginForm()) {
            @Html.AntiForgeryToken()
            <div>
                <input type="submit" value="Delete" class="btn btn-default" id="DeleteButton" />
            </div>
            <br />
            <div>
                @Html.ActionLink("Back to List", "Index")
            </div>
        }
    </div>
</body>
</html>
