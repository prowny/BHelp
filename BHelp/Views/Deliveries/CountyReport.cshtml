@using System.Web.Mvc.Html
@using BHelp.ViewModels

@model ReportsViewModel
@{ Layout = null; }
<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>CountyReport</title>
    <script src="~/Scripts/jquery-3.3.1.min.js"></script>
    <script src="~/Scripts/jquery-ui-1.12.1.min.js"></script>
    <link rel="stylesheet" href="~/Content/themes/base/jquery-ui.min.css" /> @*// for date-picker*@
    @*Put page javascript in a separate .js file where breaks can be set during debugging:*@
    <script src="@Url.Content("/Scripts/BHelpFunctions.js")"></script>
    <style>
        #Year{ width: 50px;}
        #Quarter{ width: 25px;}
    </style>
</head>
<body>
<table>
    <tr><td><b>County Report</b></td></tr>
    <tr>
        <td style="text-align: right">Select Year:</td>
        <td>@Html.EditorFor(m => m.Year, new {htmlAttributes = new{@min="2020", @max="2050"}, id = "Year" })</td>&nbsp;&nbsp;
        <td style="text-align: right">Select Quarter:</td>
        <td>@Html.EditorFor(m => m.Quarter, new { htmlAttributes = new { @min = "1", @max = "4" }, id = "Quarter"})</td>
    </tr>
</table>
<br/>
<table>
    <tr><td><b>Bethesda Help, Inc.</b></td></tr>
    <tr><td><b>@Html.Label(Model.DateRangeTitle)</b></td></tr>
    <tr></tr>
</table>
<table id="Month0">
    <tr>
        <td style="color: red; text-align: center;">@Html.Label(Model.MonthYear[0])</td>
        @for(int i = 0; i < Model.ZipCodes.Count; i++)
        {<td></td>}
        <td style="color: red; text-align: center;">@Html.Label("TOTAL")</td>
    </tr>
    <tr>
        <td style="text-align: left">Zip Code</td>
        @foreach(var zip in Model.ZipCodes)
        {<td style="text-align: right">@zip</td>}
        <td style="text-align: center">All Zip Codes</td>
    </tr>
    <tr><td style="text-align: right"># of Families</td></tr>
    @*@for(int i = Model.)*@
    <tr><td style="text-align: right"># of Children (&#60;18)</td></tr>
    <tr><td style="text-align: right"># of Adults(&#62;=18 and &#60;60)</td></tr>
    <tr><td style="text-align: right"># of Seniors (&#62;=60)</td></tr>
    <tr><td style="text-align: right"># of Residents</td></tr>
    <tr><td style="text-align: right"># of Pounds of Food</td></tr>
    <tr></tr>
</table>
<table id="Month1">
    <tr>
        <td style="color: red; text-align: center">@Html.Label(Model.MonthYear[1])</td>
        @for(int i = 0; i < Model.ZipCodes.Count; i++)
        {<td></td>}
        <td style="color: red; text-align: center;">@Html.Label("TOTAL")</td>
    </tr>
    <tr>
        <td style="text-align: left">Zip Code</td>
        @foreach(var zip in Model.ZipCodes)
        {<td style="text-align: right">@zip</td>}
        <td style="text-align: center">All Zip Codes</td>
    </tr>
    <tr><td style="text-align: right"># of Families</td></tr>
    <tr><td style="text-align: right"># of Children (&#60;18)</td></tr>
    <tr><td style="text-align: right"># of Adults(&#62;=18 and &#60;60)</td></tr>
    <tr><td style="text-align: right"># of Seniors (&#62;=60)</td></tr>
    <tr><td style="text-align: right"># of Residents</td></tr>
    <tr><td style="text-align: right"># of Pounds of Food</td></tr>
    <tr></tr>
</table>
<table id="Month2">
    <tr>
        <td style="color: red; text-align: center">@Html.Label(Model.MonthYear[2])</td>
        @for(int i = 0; i < Model.ZipCodes.Count; i++)
        {<td></td>}
        <td style="color: red; text-align: center;">@Html.Label("TOTAL")</td>
    </tr>
    <tr>
        <td style="text-align: left">Zip Code</td>
        @foreach(var zip in Model.ZipCodes)
        {<td style="text-align: right">@zip</td>}
        <td style="text-align: center">All Zip Codes</td>
    </tr>
    <tr><td style="text-align: right"># of Families</td></tr>
    <tr><td style="text-align: right"># of Children (&#60;18)</td></tr>
    <tr><td style="text-align: right"># of Adults(&#62;=18 and &#60;60)</td></tr>
    <tr><td style="text-align: right"># of Seniors (&#62;=60)</td></tr>
    <tr><td style="text-align: right"># of Residents</td></tr>
    <tr><td style="text-align: right"># of Pounds of Food</td></tr>
</table>

<br/>
<div>
    @Html.ActionLink("Return to Reports Menu", "ReturnToReportsMenu", new {})
<br/>
    @Html.ActionLink("Return to Dashboard", "ReturnToDashboard", new {})
</div>
</body>
</html>
